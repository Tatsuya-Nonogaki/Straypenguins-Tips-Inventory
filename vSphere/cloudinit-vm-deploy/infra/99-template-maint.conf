# Prevent cloud-init from running on the template VM.
# This file disables all automatic cloud-init stages for template maintenance mode.

# Disable all init stages (init, config, final, modules)
disable_cloud_init: true
disable_cloud_config: true
disable_cloud_final: true
preserve_hostname: true
manage_etc_hosts: false

